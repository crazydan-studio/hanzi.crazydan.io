<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- 图片分享临时目录（Platform.shareImage 写入缓存后经 FileProvider 分享） -->
    <cache-path name="share" path="share/" />
    <!-- 下载目录（更新安装包 APK/笔画数据库，经 FileProvider 触发系统安装或读取） -->
    <files-path name="downloads" path="downloads/" />
</paths>
